#include <stdio.h>
/**
*main - entry point
*Return: 0 (successfull)
*/
int main(void)
{
char ch = 'a';
char cb = 'A'
for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
for (cb = 'A'; ch <= 'Z'; ch++)
putchar(ch);
putchar('\n');
return (0);
}
